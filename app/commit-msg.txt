feat: invite checkbox in the grant-access modal

The grant-access modal now offers "Send an email invitation": when on,
the password field is hidden and the person receives an email link to
set their own password (server issues a 72h token). When off, behaviour
is unchanged (admin sets the password). sendInvite is threaded through
grantAccess; GrantAccessInput.password becomes optional.
