feat: planning mode — focused per-meeting assignment flow

A "Plan" button inside each meeting block opens a focused overlay for
that meeting (the brother's zone): a progress bar, a "needs assigning"
group and a "draft, not published" group. Tapping a row opens the same
bottom sheet; when nothing is left to assign, a soft "publish this
meeting" bar appears. One meeting at a time, gated by the midweek/
weekend edit permission, so brothers plan only their own zone.
