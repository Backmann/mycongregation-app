chore: remove dead schedule-import upload client method

scheduleImportApi.upload (POST /schedule-import/upload) is no longer
called: EPUB parsing for both MWB and Watchtower now happens in the
browser, and apply sends the derived payload to /mwb-import/apply. The
server route and its parser remain as a fallback; only the unused client
method is removed.
