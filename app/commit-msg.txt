fix: keep selected schedule week in the URL

The schedule week was local component state, so any remount (e.g.
returning from the assignment screen) reset it to the current week and
collapsed the open meeting block. The week now lives in a ?week=
URL param: it survives remounts and is shareable. No change to how the
week navigator behaves for the user.
