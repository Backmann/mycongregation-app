feat: "Next" button to jump to the next empty part in planning mode

AssignmentSheet gains an optional onNext prop: when provided (only from
planning mode), a "Next" button appears beside Close to jump straight to
the next unassigned part. PlanningMode computes the next empty part from
the live zone items and wires onNext. Outside planning mode the prop is
absent, so the sheet is unchanged everywhere else.
