feat: edit assignments in a bottom sheet instead of a separate screen

Tapping a part now opens a bottom sheet over the schedule instead of
navigating to /schedule/[id]. The week, scroll position and open block
stay put, so filling a week no longer bounces you back to the current
week. Pickers save optimistically — the row fills in immediately while
the request flies in the background. The [id] screen stays for direct
links. New AssignmentSheet component wraps the existing AssignmentForm;
onEdit is threaded through MidweekSections for the midweek rows.
