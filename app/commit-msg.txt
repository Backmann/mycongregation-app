chore: drop unused canManageAbsences from home screen

After the Absences tile became visible to everyone (show: true), the
canManageAbsences permission was no longer read on the home screen,
leaving an unused variable and an orphaned usePermissions import. Both
removed to clear the lint warning.
