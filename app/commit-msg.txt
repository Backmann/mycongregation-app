fix: save public-talk pick immediately

Picking a public talk only updated local form state; unlike the
publisher pickers it never called onInstantSave, so in the new bottom
sheet (which has no submit button) the choice was lost on close — the
row fell back to the generic "Public talk" label. handleTalkSelect now
instant-saves publicTalkId and the derived part title, fixing both the
sheet and the detail screen.
