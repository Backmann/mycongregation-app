fix: add partKey to MyAssignmentItem type

The previous client change skipped this field because the generic guard
matched partKey already present on other interfaces, so MyAssignmentItem
never got it and tsc failed in my-tasks.ts. Added partKey to the correct
interface.
