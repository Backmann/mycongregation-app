feat: notify / silent publish buttons in planning mode

Planning mode now offers two clear actions when a meeting is ready: a
primary "Publish and notify" (push to the brothers) and an unobtrusive
"Publish silently" (status only, no push). notify is threaded through
assignmentsApi.publish and publishMeetingNow.
