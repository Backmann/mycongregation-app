feat: group My Tasks into per-meeting cards

Tasks are now grouped into a card per meeting (date + event type), with
the date/time/type shown once in the card header and the parts listed
below in program order, each with its section subheading. This removes
the repeated date on every row and reads like a mini-program. Cleaning
(a weekly task) gets its own card. The schedule screen is unchanged.
